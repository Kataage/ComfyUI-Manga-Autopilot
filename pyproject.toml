[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "comfyui-manga-autopilot"
version = "0.0.1"
description = "ComfyUI extension for end-to-end automated manga / Webtoon production."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
    { name = "Kataage" },
]
keywords = ["comfyui", "manga", "webtoon", "autopilot", "stable-diffusion"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Topic :: Multimedia :: Graphics",
]
dependencies = [
    "aiohttp>=3.9",
    "pydantic>=2.6",
    "PyYAML>=6.0",
    "Pillow>=10.0",
    "jsonschema>=4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-aiohttp>=1.0",
    "ruff>=0.4",
]

[project.urls]
Homepage = "https://github.com/Kataage/ComfyUI-Manga-Autopilot"
Issues = "https://github.com/Kataage/ComfyUI-Manga-Autopilot/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests/backend"]
asyncio_mode = "auto"
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]
