# Runtime dependencies for ComfyUI installs.
#
# ComfyUI (and ComfyUI-Manager) installs a node pack's `requirements.txt`;
# neither reads `pyproject.toml`. Without this file, dropping this checkout
# into `custom_nodes/` leaves `jsonschema` missing from ComfyUI's
# environment. `register_all()` then raises `ModuleNotFoundError`, and while
# the failure is logged, the sidebar tab still renders - so the extension
# looks installed while every HTTP route is absent.
#
# Keep in sync with `[project].dependencies` in pyproject.toml;
# tests/backend/test_comfyui_requirements.py enforces that.
aiohttp>=3.9
pydantic>=2.6
PyYAML>=6.0
Pillow>=10.0
jsonschema>=4.0
