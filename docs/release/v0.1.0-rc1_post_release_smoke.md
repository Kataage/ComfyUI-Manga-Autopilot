# v0.1.0-rc1 Post-release Smoke Verification

## Release

- **Tag:** v0.1.0-rc1
- **Commit:** 93284c33eddcb05a5bac108a166321d793358a15
- **GitHub Release:** https://github.com/Kataage/ComfyUI-Manga-Autopilot/releases/tag/v0.1.0-rc1
- **Pre-release:** Yes
- **Verification date:** 2026-06-13

## Fresh clone verification

```
git clone https://github.com/Kataage/ComfyUI-Manga-Autopilot.git
git checkout v0.1.0-rc1
git rev-parse HEAD → 93284c33eddcb05a5bac108a166321d793358a15
```

**Result:** PASS — HEAD matches expected commit.

## Install verification

```
pip install -e .
python -c "import manga_autopilot; print(manga_autopilot.__version__)"
→ 0.1.0-rc1
```

**Result:** PASS — Install succeeds, version correct, no external service required.

## Test verification

```
ruff check .                    → All checks passed
pytest -m release_gate -q       → 6 passed
pytest test_release_readiness.py → 18 passed
pytest tests/backend/ -q        → 809 passed, 15 skipped
```

**Result:** PASS — All tests pass, no external service required.

## Opt-in integration skip verification

| Category | Skip env var | Default |
|----------|-------------|---------|
| Real ComfyUI E2E | `MANGA_AUTOPILOT_REAL_COMFYUI_E2E` | Skipped |
| Real Modal worker E2E | `MANGA_AUTOPILOT_REAL_MODAL_E2E` | Skipped |
| Real Modal ComfyUI E2E | `MANGA_AUTOPILOT_REAL_MODAL_COMFYUI_E2E` | Skipped |
| Real S3/R2 artifact store E2E | `MANGA_AUTOPILOT_REAL_S3_E2E` | Skipped |
| Real S3/R2 signed URL E2E | `MANGA_AUTOPILOT_REAL_S3_E2E` + `MANGA_AUTOPILOT_ARTIFACT_ACCESS_MODE` | Skipped |

**Result:** PASS — All 15 opt-in E2E tests skip by default.

## Documentation verification

- Release notes path: `docs/release/v0.1.0-rc1_release_notes.md` ✓
- Acceptance matrix path: `docs/release/v1_acceptance_matrix.md` ✓
- README mentions v0.1.0-rc1 (via v0.x → v1.0.0 status) ✓
- Modal / R2 / S3 sections state opt-in ✓
- No docs claim automatic model download ✓
- No docs include real secrets or credentials ✓

**Result:** PASS — All documentation links and references verified.

## Security and model file scan

- `git grep` for real secrets: No real keys found
- `.safetensors` / `.ckpt` / `.pt` / `.pth` files: None
- `.env` / `.env.*` files: None
- Placeholder examples use `...` (clearly fake)

**Result:** PASS — No security or model file risks.

## Result

**PASSED** — v0.1.0-rc1 release is verified as usable from a fresh clone.
No release-blocking issues found.
